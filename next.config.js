/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA será configurado via next-pwa se necessário, mas vamos fazer manualmente
}

module.exports = nextConfig
